var React = require('react');  
var RouteHandler = require('react-router').RouteHandler;


let App = React.createClass({

  render: function () {
    return (
      <RouteHandler />
    );
  }
});

export default App;
